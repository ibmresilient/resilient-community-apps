[resilient]
host=localhost
email=a@example.com
password=$password
port=1443
org=dev

[fn_netdevice]
#selftest=
# specify the directory if using textFSM templates
#template_dir=/Users/markscherfling/dev/ntc-templates-master/templates

# for each device to communicate with, define it's section below to match the device_ids field in the function input parameter
[ansible-linux]
device_type=linux
ip=192.168.56.3
#port=22
username=root
password=$password
#secret=<leave commented for default of ''>
verbose=False
use_commit=False