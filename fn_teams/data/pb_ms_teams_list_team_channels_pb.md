<!--
    DO NOT MANUALLY EDIT THIS FILE
    THIS FILE IS AUTOMATICALLY GENERATED WITH resilient-sdk codegen
    Generated with resilient-sdk v51.0.7.0.1603
-->

# Playbook - MS Teams: List Team Channels (PB)

### API Name
`ms_teams_list_team_channels_pb`

### Status
`enabled`

### Activation Type
`Manual`

### Activation Conditions
`-`

### Activation Form Elements
| Input Field Label | API Name | Element Type | Tooltip | Requirement |
| ----------------- | -------- | ------------ | ------- | ----------- |
| Channel Team Id | `ms_channel_team_id` | text | Unique id assigned to a MS Team while being created | Always |
| Filter | `teams_filter` | text | Optional filter values | Optional |

### Object Type
`incident`

### Description
List available team channels, using an optional filter to reduce the list returned.


---
## Function - MS Teams: List Team Channels

### API Name
`ms_teams_list_team_channels`

### Output Name
`list_channel_result`

### Message Destination
`fn_teams`

### Function-Input Script
```python
if hasattr(playbook.inputs, "teams_filter"):
  inputs.teams_filter = playbook.inputs.teams_filter
inputs.ms_channel_team_id = playbook.inputs.ms_channel_team_id
```

---

## Local script - List Team Channels Post Processing Script

### Description


### Script Type
`Local script`

### Object Type
`incident`

### Script Content
```python
FN_NAME = "ms_teams_list_team_channels"
WF_NAME = "MS Teams: List Team Channels (PB)"

results = playbook.functions.results.list_channel_result
content = results.get("content", {})

if not results.get("success"):
  text = f"MS Teams Playbook: <b>{WF_NAME}</b><br>Failed for SOAR function <b>{FN_NAME}</b><br>"
  text += f"Unable to List Microsoft Team Channels"
  fail_reason = results.get("reason")
  if fail_reason:
    text = f"""{text}:\n\tFailure reason: {fail_reason}
    Inputs: {str(playbook.inputs)}"""

else:
  text = f"MS Teams Playbook: <b>{WF_NAME}</b><br>Successfully Executed for SOAR function <b>{FN_NAME}</b><br>"
  for ms_group in content:
    row = incident.addRow("ms_teams_list_team_channels")
    row["channel_id"] = ms_group.get("id")
    row["channel_name"] = ms_group.get("displayName")
incident.addNote(helper.createRichText(text))
```

---

