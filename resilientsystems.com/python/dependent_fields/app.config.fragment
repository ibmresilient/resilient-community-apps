# -----------------------------------------------------------------------------
# The 'time delta' action component
# -----------------------------------------------------------------------------
[timedelta1]
# The queue we want to listen to
queue=timedelta
# Pick the custom fields to lookup and write to
source_field=custom3
source_field2=custom4
dest_field=custom5
