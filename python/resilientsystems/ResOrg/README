Python module that provides various utility methods for interaction with a Resilient server.  

Installation
==================================
Install directly into the local python environment:
    python setup.py install

If using a virtual environment, or developing enhancements
    python setup.py develop

Creating a pip installable package
    python setup.py sdist --formats=gztar

    This creates a singlefile `dist/ResilientOrg-x.x.x.tar.gz  (where x.x.x is the version number in the repository)

    Install with
        pip install ResilientOrg-x.x.x.tar.gz